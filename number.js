const { min } = require("lodash");

const numb = "51010101";

function splitNumber() {
    let a = numb.charAt(0)
    let b = numb.
}

function split(){
    for (let index = 0; index < numb.length; index++) {
        if (index === 0) {
            let a = numb.charAt(index)
        }else if(index === 1){
            let b = numb.charAt(index)
        }else if(index > 1 && index <3){
            let b = numb.charAt(index)
        }else if()
        const element = ;
        
    }
}
splitNumber();
